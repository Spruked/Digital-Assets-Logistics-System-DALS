# Master Docker Compose for Complete DALS Ecosystem
# Includes DALS Core + CertSig + TrueMark + Cali_X_One Voice AI Integration

version: '3.8'

services:
  # ============================================================================
  # DALS CORE SERVICES
  # ============================================================================

  # DALS Controller Service (Main ISS Module)
  dals-controller:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    image: dals-controller:v1.0.0
    container_name: dals-controller
    ports:
      - "8003:8003"  # API Gateway
      - "8008:8008"  # Dashboard Server
    environment:
      - ENVIRONMENT=production
      - ISS_SERVICE_NAME=dals-controller
      - ISS_HOST=0.0.0.0
      - ISS_PORT=8003
      - DASHBOARD_PORT=8008
      - LOG_LEVEL=INFO
      - LOG_FORMAT=json
      - CALI_X_ONE_INTEGRATION_READY=true
      - REDIS_URL=redis://redis:6379
      - REDIS_ENABLED=true
      - MODULE_STATUS_TRACKING=true
      - WEBSOCKET_ENABLED=true
      - DALS_001_ENFORCED=true
      # Phase 11-A2: Autonomous Predictive Prevention
      - PREDICTIVE_ENGINE_ENABLED=true
      - AUTONOMOUS_PREVENTION_MODE=11-A2
      - PREDICTIVE_SCAN_INTERVAL=5
      - RISK_THRESHOLD_HIGH=70
      - RISK_THRESHOLD_CRITICAL=90
      - SELF_MODEL_ENABLED=true
      - AWARENESS_LAYER_ACTIVE=true
      - VOICE_AWARENESS_ENABLED=true
      - CANS_AUTONOMOUS_MODE=aggressive
      - PREDICTIVE_FAILURE_MODELING=true
      - HEALTH_TREND_ANALYSIS=true
      - PREVENTION_PROTOCOLS_ACTIVE=true
      # Cali_X_One Voice AI Integration
      - CALI_X_ONE_ENABLED=true
      - HOST_BUBBLE_ENABLED=true
      - SPEECH_RECOGNITION_ENABLED=true
      - ELEVENLABS_VOICE_ENABLED=true
      - WEBSOCKET_COMMUNICATION=true
      # Worker Vault System
      - WORKER_VAULT_ENABLED=true
      - WORKER_INVENTORY_VAULT_PATH=/app/vault/worker_inventory
      - ACTIVE_WORKERS_VAULT_PATH=/app/vault/active_workers
      - WORKER_DEPLOYMENT_TRACKING=true
      - WORKER_PERFORMANCE_MONITORING=true
      - VAULT_AUTO_BACKUP_ENABLED=true
      - VAULT_BACKUP_INTERVAL_SECONDS=21600
      # Worker Registry & Cognitive Flywheel
      - WORKER_REGISTRY_ENABLED=true
      - CALEON_FUSION_ENABLED=true
      - PREDICATE_BROADCASTING=true
      - MICRO_SKG_ENABLED=true
      - UQV_ENABLED=true
      - FUSION_THRESHOLD=0.65
      - INVENTION_THRESHOLD=0.75
      # Voice & Communication
      - CALI_VOICE_ENGINE=gtts
      - CALI_SECURITY_LAYER=active
      - CALI_METAMASK_INTEGRATION=true
      # Cali Ethics Gate Integration
      - CALI_ETHICS_URL=http://cali-ethics:8006
      - PHI3_ENDPOINT=http://phi-3-mini:8005
      - ETHICS_THRESHOLD=0.80
      - STREAM_TIMEOUT=30.0
      # Minting Service URLs
      - CERTSIG_BACKEND_URL=https://api.certsig.com
      - TRUEMARK_BACKEND_URL=https://api.truemarkmint.com
    volumes:
      - ./iss_module/data:/app/data
      - ./vault:/app/vault
      - ./logs:/app/logs
      - ./config:/app/config:ro  # Mount config for cochlear monitor
      - ./iss_module/cali_x_one:/app/cali_x_one  # Cali_X_One voice AI integration
    depends_on:
      - redis
      - cali-ethics
      - phi-3-mini
      - alphacertsig-backend
      - truemark-backend
    networks:
      - dals-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8003/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # Cali Ethics Gate - Side-car for token-level ethics filtering
  cali-ethics:
    build:
      context: .
      dockerfile: Dockerfile.cali-ethics
      target: ethics-gate
    image: cali-ethics:v1.1.0
    container_name: cali-ethics
    ports:
      - "8006:8006"  # Cali ethics gate API
    environment:
      - ETHICS_THRESHOLD=0.80
      - PHI3_ENDPOINT=http://phi-3-mini:8005
      - PORT=8006
    volumes:
      - ./iss_module/cali_x_one/models:/models:ro  # Ethics model weights
    networks:
      - dals-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8006/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    depends_on:
      - phi-3-mini

  # Phi-3-mini - Side-car for token streaming articulation
  phi-3-mini:
    image: ghcr.io/microsoft/phi-3-mini:4bit-stream
    container_name: phi-3-mini
    ports:
      - "8005:8005"  # Phi-3 streaming endpoint
    environment:
      - PORT=8005
      - MAX_SEQ_LEN=512
      - STREAM_MODE=true
      - LOGITS_OUTPUT=true
    networks:
      - dals-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8005/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M

    # Redis for caching and session storage - Pinned version
  redis:
    image: redis:7.2-alpine
    container_name: dals-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - dals-network
    restart: unless-stopped
    command: redis-server --appendonly yes

  # ============================================================================
  # NFT MINTING SERVICES
  # ============================================================================

  # Alpha CertSig Elite Backend
  alphacertsig-backend:
    build:
      context: ./alpha-certsig/backend
      dockerfile: Dockerfile
    image: alphacertsig-backend:v1.0.0
    container_name: alphacertsig-backend
    ports:
      - "9000:8080"  # Exposed on 9000 to avoid conflicts
    environment:
      - NODE_ENV=production
      - PORT=8080
      - REDIS_URL=redis://redis:6379
      - IPFS_NODE_URL=https://ipfs.infura.io:5001
      - WEB3_PROVIDER_URL=https://polygon-mainnet.infura.io/v3/YOUR_PROJECT_ID
      - CONTRACT_ADDRESS=0x1234567890123456789012345678901234567890
    volumes:
      - ./alpha-certsig/backend/app:/app
      - ./alpha-certsig/shared/contracts:/app/contracts:ro
      - ./alpha-certsig/backend/logs:/app/logs
    depends_on:
      - redis
    networks:
      - dals-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Alpha CertSig Elite Frontend
  alphacertsig-frontend:
    build:
      context: ./alpha-certsig/frontend
      dockerfile: Dockerfile
    image: alphacertsig-frontend:v1.0.0
    container_name: alphacertsig-frontend
    ports:
      - "3001:80"  # Changed from 3000 to avoid conflict with Shiloh Ridge Farm
    environment:
      - NODE_ENV=production
      - REACT_APP_BACKEND_URL=https://api.certsig.com
      - REACT_APP_DALS_API_URL=https://spruked.com
    depends_on:
      - alphacertsig-backend
    networks:
      - dals-network
    restart: unless-stopped

  # TrueMark Mint Backend
  truemark-backend:
    build:
      context: ./truemark-mint
      dockerfile: Dockerfile.backend
    image: truemark-backend:v1.0.0
    container_name: truemark-backend
    ports:
      - "9001:5000"  # Exposed on 9001 to avoid conflicts
    environment:
      - FLASK_ENV=production
      - DATABASE_URL=sqlite:///data/truemark.db
      - REDIS_URL=redis://redis:6379
      - IPFS_NODE_URL=https://ipfs.infura.io:5001
      - WEB3_PROVIDER_URL=https://mainnet.infura.io/v3/YOUR_PROJECT_ID
      - CONTRACT_ADDRESS=0x1234567890123456789012345678901234567890
    volumes:
      - ./truemark-mint/Alpha-mint-engine:/app
      - ./truemark-mint/data:/app/data
      - ./truemark-mint/logs:/app/logs
    depends_on:
      - redis
    networks:
      - dals-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # ============================================================================
  # WORKER FLEET (AUTONOMOUS AI AGENTS)
  # ============================================================================
  
  # Josephine - TrueMark NFT Mint Specialist (DMN-TM-01)
  josephine-worker:
    build:
      context: ./worker_templates
      dockerfile: Dockerfile.worker
    image: host-bubble-worker:v1.0.0
    container_name: josephine-default
    environment:
      - WORKER_NAME=Josephine
      - WORKER_ID=josephine-default
      - WORKER_PORT=8080
      - TARGET_USER_ID=default_user
      - CALI_X_ONE_API=http://dals-controller:8003
      - TRUEMARK_API=http://dals-controller:8003/api/truemark
      - SKG_API=http://localhost:8002
      - UQV_API=http://dals-controller:8003/api/uqv
      - TTS_ENDPOINT=http://dals-controller:8003/api/voice/tts
      - CHAT_ENDPOINT=http://dals-controller:8003/api/chat
    volumes:
      - ./worker_templates:/app
    depends_on:
      - dals-controller
      - redis
    networks:
      - dals-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M

  # TrueMark Mint Frontend
  truemark-frontend:
    build:
      context: ./truemark-mint
      dockerfile: Dockerfile.frontend
    image: truemark-frontend:v1.0.0
    container_name: truemark-frontend
    ports:
      - "8082:8080"  # Changed from 8081 to avoid conflict with UCM
    environment:
      - NODE_ENV=production
      - REACT_APP_BACKEND_URL=https://api.truemarkmint.com
      - REACT_APP_DALS_API_URL=https://spruked.com
    depends_on:
      - truemark-backend
    networks:
      - dals-network
    restart: unless-stopped

  # ============================================================================
  # COGNITIVE SERVICES
  # ============================================================================

  # Cali_X_One Voice AI Integration (No UCM - Sovereign AI Only)
  # Cali_X_One provides voice commands and MetaMask-signed identity verification
  # Integrated directly into DALS controller via volume mount

  # Service Discovery (Consul)
  consul:
    image: consul:1.15
    container_name: iss-consul
    ports:
      - "8500:8500"
    environment:
      - CONSUL_BIND_INTERFACE=eth0
    volumes:
      - consul_data:/consul/data
    networks:
      - dals-network
    restart: unless-stopped
    command: consul agent -dev -client=0.0.0.0 -ui

  # Log aggregation with Grafana Loki (optional)
  loki:
    image: grafana/loki:2.9.0  # Pinned version
    container_name: dals-loki
    ports:
      - "3100:3100"
    volumes:
      - ./monitoring/loki-config.yaml:/etc/loki/local-config.yaml
      - loki_data:/loki
    networks:
      - dals-network
    restart: unless-stopped
    command: -config.file=/etc/loki/local-config.yaml

networks:
  dals-network:
    driver: bridge
    name: dals-phase1-network

volumes:
  redis_data:
    driver: local
    name: dals-redis-data
  consul_data:
    driver: local
    name: dals-consul-data
  loki_data:
    driver: local
    name: dals-loki-data